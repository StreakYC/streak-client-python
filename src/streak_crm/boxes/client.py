# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawBoxesClient, RawBoxesClient


class BoxesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawBoxesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawBoxesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawBoxesClient
        """
        return self._raw_client

    def get_box_markdown(
        self,
        box_key: str,
        *,
        max_tokens: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> str:
        """
        Returns the box as one markdown document: an overview, its columns, contacts, organizations, linked boxes, tasks and pipeline, followed by its timeline. Every timestamp is ISO-8601 UTC. The document's structure is best-effort and may change; use the JSON endpoints for a stable contract.

        Parameters
        ----------
        box_key : str

        max_tokens : typing.Optional[int]
            A token cap for the entire document, counted with OpenAI's o200k_base encoding. It covers the box sections first, then uses the remaining tokens for the timeline. A marker shows where entries were left out. If the box does not fit under the cap, a 400 error shows the minimum it needs. Omit the cap to return the full box.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        str
            The box rendered as one markdown document.

        Examples
        --------
        from streak_crm import Streak

        client = Streak(
            token="YOUR_TOKEN",
        )
        client.boxes.get_box_markdown(
            box_key="boxKey",
        )
        """
        _response = self._raw_client.get_box_markdown(box_key, max_tokens=max_tokens, request_options=request_options)
        return _response.data


class AsyncBoxesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawBoxesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawBoxesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawBoxesClient
        """
        return self._raw_client

    async def get_box_markdown(
        self,
        box_key: str,
        *,
        max_tokens: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> str:
        """
        Returns the box as one markdown document: an overview, its columns, contacts, organizations, linked boxes, tasks and pipeline, followed by its timeline. Every timestamp is ISO-8601 UTC. The document's structure is best-effort and may change; use the JSON endpoints for a stable contract.

        Parameters
        ----------
        box_key : str

        max_tokens : typing.Optional[int]
            A token cap for the entire document, counted with OpenAI's o200k_base encoding. It covers the box sections first, then uses the remaining tokens for the timeline. A marker shows where entries were left out. If the box does not fit under the cap, a 400 error shows the minimum it needs. Omit the cap to return the full box.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        str
            The box rendered as one markdown document.

        Examples
        --------
        import asyncio

        from streak_crm import AsyncStreak

        client = AsyncStreak(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.boxes.get_box_markdown(
                box_key="boxKey",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_box_markdown(
            box_key, max_tokens=max_tokens, request_options=request_options
        )
        return _response.data
